const cards=[...document.querySelectorAll('.brief-card')],search=document.getElementById('search'),filters=[...document.querySelectorAll('.filter')];let active='All';function run(){const q=search.value.trim().toLowerCase();cards.forEach(c=>{c.style.display=((active==='All'||c.dataset.cat===active)&&(!q||c.dataset.search.includes(q)))?'flex':'none'})}search.addEventListener('input',run);filters.forEach(b=>b.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');active=b.dataset.cat;run()}));